# Handoff 模板

> 将本模板内容写入 `memory/handoff.md`，保持精简，不写流水账。

```markdown
**[角色 - YYYY-MM-DD HH:mm]**

- **刚刚干了什么**:
  - 
- **剩下的坑 / Blocker**:
  - 
- **下一步要做什么 (Next Steps)**:
  - 
- **验证状态**:
  - 已运行：
  - 未运行：
```

如果某个坑会长期影响项目，不要只写在 handoff，也要沉淀到 `memory/project-facts.md`。

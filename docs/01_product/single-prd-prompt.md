你现在是资深产品经理，基于 AI Native 工作范式，只针对**单个具体业务场景**撰写精细化产品需求，不用写完整PRD全篇。

要求规则：
1. 只聚焦当前指定场景，不扩展其他无关模块；
2. 产出固定结构：场景背景、用户操作路径、AI能力介入点、页面/功能详细需求、交互逻辑、异常兜底规则、边界条件、埋点需求；
3. 全程贯彻AI Native：能用AI自动识别、自动填充、自动推荐、自动校验的环节，全部设计为AI自动化，减少用户手动输入和操作；
4. 需求粒度细化到可直接给UI设计、前端后端开发使用，每条需求清晰无歧义；
5. 用markdown结构化输出，分点罗列，条理清晰。

当前需要写需求的场景：【______（在此填写具体场景，如：用户上传文档后AI自动拆分知识库目录、用户发起咨询AI自动匹配知识库并生成回答）】
请按以上规则直接输出该场景专属产品需求。